# ToDo API с матрицей Эйзенхауэра 3.0.0

REST API для управления задачами с использованием матрицы Эйзенхауэра, разработанное на FastAPI с интеграцией PostgreSQL.

## 👨‍💻 Автор

**Грошев Роман**

## 📋 Описание

Полнофункциональное API для управления списком задач (ToDo List) с поддержкой категоризации задач по матрице Эйзенхауэра. Система автоматически распределяет задачи на 4 квадранта в зависимости от их важности и срочности:

- **Q1** - Важные и срочные (требуют немедленного внимания)
- **Q2** - Важные, но не срочные (стратегическое планирование)
- **Q3** - Не важные, но срочные (делегирование)
- **Q4** - Не важные и не срочные (можно отложить)

### ✨ Ключевые возможности

- 🗄️ **PostgreSQL** интеграция с асинхронными запросами (Supabase)
- 🔐 **Аутентификация и авторизация** через JWT токены
- 👥 **Система ролей** (user, admin) с контролем доступа
- 📅 **Автоматическое определение срочности** на основе дедлайнов
- ⏰ **Планировщик задач** для синхронизации статусов в реальном времени
- 🔄 **Полный CRUD** для управления задачами
- 📊 **Расширенная статистика** и аналитика по задачам
- 🔍 **Поиск и фильтрация** задач по различным критериям
- ✅ **Валидация данных** с помощью Pydantic
- 👑 **Админ-панель** для управления пользователями и системой
- 🔀 **Поддержка API v2 и v3** (обратная совместимость)

## 🚀 Технологии

- **FastAPI** 0.119.0 - высокопроизводительный веб-фреймворк
- **SQLAlchemy** (async) - асинхронный ORM
- **PostgreSQL** - реляционная база данных (Supabase)
- **Pydantic** 2.12.0 - валидация и сериализация данных
- **APScheduler** 3.10.4 - асинхронный планировщик задач
- **Uvicorn** 0.37.0 - ASGI сервер
- **PyJWT** - работа с JWT токенами
- **Passlib + Bcrypt** - безопасное хеширование паролей
- **python-multipart** - обработка форм
- **email-validator** - валидация email адресов

## 📦 Установка

### 1. Клонируйте репозиторий
```bash
git clone <url-репозитория>
cd ToDo-API-BSBO
```

### 2. Создайте виртуальное окружение
```bash
python -m venv .venv
source .venv/bin/activate  # для macOS/Linux
# или
.venv\Scripts\activate  # для Windows
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:
```env
# ===== Database =====
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/todo_db

# ===== JWT Settings =====
SECRET_KEY=your-super-secret-key-change-in-production-12345
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ===== Optional =====
DEBUG=true
```

**Для использования Supabase:**
1. Создайте проект на [supabase.com](https://supabase.com)
2. Получите строку подключения в Settings → Database → Connection string
3. Замените `postgresql://` на `postgresql+asyncpg://`
4. Пример: `postgresql+asyncpg://postgres:[password]@db.xyz.supabase.co:5432/postgres`

### 5. Проверьте подключение к БД
```bash
python test_connection.py
```

## 🎯 Запуск

### Режим разработки (с автоперезагрузкой)
```bash
uvicorn main:app --reload
```

### Продакшн режим
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

API будет доступно по адресу: `http://127.0.0.1:8000`

При запуске автоматически:
- ✅ Создаются таблицы в БД (если их нет)
- ✅ Запускается планировщик для синхронизации срочности задач

## 📚 Документация API

После запуска сервера интерактивная документация доступна:
- **Swagger UI**: `http://127.0.0.1:8000/docs`
- **ReDoc**: `http://127.0.0.1:8000/redoc`
- **OpenAPI JSON**: `http://127.0.0.1:8000/openapi.json`

## 🔌 API Эндпоинты

> **Примечание**: API поддерживает версии v2 и v3 (v3 рекомендуется для новых интеграций)

### 🟢 Публичные эндпоинты (без авторизации)

- `GET /` - Приветствие и информация об API
- `GET /health` - Проверка здоровья системы и БД

### 🔐 Аутентификация (`/api/v3/auth`)

- `POST /api/v3/auth/register` - Регистрация нового пользователя
- `POST /api/v3/auth/login` - Вход и получение JWT токена
- `GET /api/v3/auth/me` - Информация о текущем пользователе (требует токен)
- `POST /api/v3/auth/logout` - Выход из системы

### 📝 Управление задачами (`/api/v3/tasks`)

Все эндпоинты требуют JWT токена в заголовке `Authorization: Bearer {token}`

#### Чтение
- `GET /api/v3/tasks/` - Получить все задачи пользователя
- `GET /api/v3/tasks/{task_id}` - Получить задачу по ID
- `GET /api/v3/tasks/quadrant/{quadrant}` - Фильтр по квадранту (Q1/Q2/Q3/Q4)
- `GET /api/v3/tasks/status/{status}` - Фильтр по статусу (completed/pending)
- `GET /api/v3/tasks/search?q={keyword}` - Поиск по названию/описанию (мин 2 символа)
- `GET /api/v3/tasks/today` - Задачи со сроком на сегодня
- `GET /api/v3/tasks/upcoming` - Наступающие задачи (в течение 7 дней)

#### Создание и изменение
- `POST /api/v3/tasks/` - Создать новую задачу
- `PUT /api/v3/tasks/{task_id}` - Полное обновление задачи
- `PATCH /api/v3/tasks/{task_id}` - Частичное обновление полей
- `PATCH /api/v3/tasks/{task_id}/complete` - Отметить как выполненную
- `PATCH /api/v3/tasks/{task_id}/uncomplete` - Отметить как невыполненную
- `DELETE /api/v3/tasks/{task_id}` - Удалить задачу

### 📊 Статистика (`/api/v3/stats`)

Требует JWT токена

- `GET /api/v3/stats/` - Общая статистика пользователя
- `GET /api/v3/stats/timing` - Анализ дедлайнов
- `GET /api/v3/stats/completion` - Статистика выполнения
- `GET /api/v3/stats/productivity` - Индекс продуктивности

### 👑 Администрирование (`/api/v2/admin`)

Только для пользователей с ролью `admin`

- `GET /api/v2/admin/users` - Список всех пользователей
- `GET /api/v2/admin/users/{user_id}` - Информация о пользователе
- `PUT /api/v2/admin/users/{user_id}/role` - Изменить роль пользователя
- `DELETE /api/v2/admin/users/{user_id}` - Удалить пользователя
- `GET /api/v2/admin/stats` - Глобальная статистика системы
- `GET /api/v2/admin/stats/users` - Статистика по пользователям

## 💡 Примеры использования

### 1. Регистрация нового пользователя
```bash
curl -X POST http://127.0.0.1:8000/api/v3/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "nickname": "John Doe",
    "email": "john@example.com",
    "password": "secure_password_123"
  }'

# Ответ:
# {
#   "id": 1,
#   "nickname": "John Doe",
#   "email": "john@example.com",
#   "role": "user",
#   "is_active": true
# }
```

### 2. Вход и получение токена
```bash
curl -X POST http://127.0.0.1:8000/api/v3/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "john@example.com",
    "password": "secure_password_123"
  }'

# Ответ:
# {
#   "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
#   "token_type": "bearer"
# }
```

### 3. Получить информацию о себе
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://127.0.0.1:8000/api/v3/auth/me
```

### 4. Создать новую задачу
```bash
curl -X POST http://127.0.0.1:8000/api/v3/tasks/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Подготовить презентацию",
    "description": "Презентация для защиты проекта",
    "is_important": true,
    "deadline_at": "2025-12-20T15:00:00Z"
  }'
```

### 5. Получить все задачи
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://127.0.0.1:8000/api/v3/tasks/
```

### 6. Фильтрация по квадранту (Q1 - срочные и важные)
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://127.0.0.1:8000/api/v3/tasks/quadrant/Q1
```

### 7. Поиск задач
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://127.0.0.1:8000/api/v3/tasks/search?q=презентация
```

### 8. Отметить задачу выполненной
```bash
curl -X PATCH http://127.0.0.1:8000/api/v3/tasks/1/complete \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 9. Получить статистику пользователя
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://127.0.0.1:8000/api/v3/stats/
```

### 10. Проверить здоровье системы
```bash
curl http://127.0.0.1:8000/health
```

## 📊 Структура проекта

```
ToDo-API-BSBO/
├── main.py                 # Точка входа, lifespan, подключение роутов
├── database.py             # Конфигурация PostgreSQL + AsyncSession
├── schemas.py              # Pydantic схемы для задач
├── schemas_auth.py         # Pydantic схемы для аутентификации
├── auth_utils.py           # Утилиты JWT и Bcrypt
├── dependencies.py         # FastAPI зависимости (get_current_user, etc)
├── models.py               # Импорт моделей
├── models/                 # SQLAlchemy модели
│   ├── __init__.py
│   ├── task.py            # Модель Task с полями задачи
│   └── user.py            # Модель User с ролями
├── routers/               # API роуты (v2 и v3)
│   ├── __init__.py
│   ├── tasks.py           # CRUD операции для задач
│   ├── stats.py           # Статистика и аналитика
│   ├── auth.py            # Аутентификация и авторизация
│   └── admin.py           # Панель администратора
├── scheduler.py           # APScheduler для автообновления срочности
├── utils.py               # Вспомогательные функции
├── test_connection.py     # Проверка подключения к БД
├── requirements.txt       # Зависимости проекта
├── .env                   # Переменные окружения (НЕ в Git!)
├── .env.example           # Пример .env (в Git)
├── .gitignore             # Игнорируемые файлы
└── README.md              # Документация (этот файл)
```

## 📝 Модель данных

### User (Пользователь)

```json
{
  "id": 1,
  "nickname": "John Doe",
  "email": "john@example.com",
  "hashed_password": "$2b$12$...",
  "role": "user",
  "is_active": true,
  "created_at": "2025-12-15T10:00:00Z",
  "updated_at": "2025-12-15T10:00:00Z"
}
```

**Роли пользователей:**
- `user` - обычный пользователь (может управлять своими задачами)
- `admin` - администратор (полный доступ к системе)

### Task (Задача)

```json
{
  "id": 1,
  "user_id": 1,
  "title": "Подготовить презентацию",
  "description": "Презентация для защиты проекта",
  "is_important": true,
  "is_urgent": true,
  "deadline_at": "2025-12-20T15:00:00Z",
  "quadrant": "Q1",
  "completed": false,
  "created_at": "2025-12-15T10:00:00Z",
  "completed_at": null
}
```

**Вычисляемые поля в API:**
- `days_until_deadline` - дней до дедлайна
- `is_overdue` - просрочена ли задача

## 🔐 Безопасность

### JWT Аутентификация
- **Стандарт**: JWT (JSON Web Token) с алгоритмом HS256
- **Срок действия**: 24 часа (настраивается в `.env`)
- **Передача**: заголовок `Authorization: Bearer {token}`

### Хеширование паролей
- **Алгоритм**: Bcrypt с 12 раундами (безопасное по стандартам 2025)
- **Принцип**: пароли никогда не хранятся в открытом виде

### Контроль доступа
- Каждый пользователь видит только свои задачи
- Админ-функции защищены проверкой роли
- Валидация всех входных данных через Pydantic

## ⏰ Планировщик задач

**APScheduler** автоматически обновляет статусы:

- ✅ **Периодическая проверка**: каждые 5 минут
- ✅ **Ежедневная синхронизация**: в 09:00 UTC
- ✅ **Логика срочности**: задача срочна если дедлайн ≤ 3 дней

### Примеры изменений при обновлении:
- Задача → Q1 (если была важна и приблизился дедлайн)
- Задача → Q2 (если важна, но дедлайн далеко)
- Задача → Q3 (если не важна, но срочна)
- Задача → Q4 (если не важна и не срочна)

## 🎯 Особенности реализации

### Асинхронная архитектура
- ⚡ Асинхронные запросы к БД через `AsyncSession`
- ⚡ Асинхронный драйвер `asyncpg` для PostgreSQL
- ⚡ Неблокирующие операции ввода-вывода
- ⚡ Высокая пропускная способность даже при большом числе запросов

### Валидация данных
- ✓ Pydantic V2 для всех моделей
- ✓ Проверка длины строк (3-100 для titles)
- ✓ Валидация email адресов
- ✓ Автоматическое преобразование типов
- ✓ Детальные сообщения об ошибках

### Многверсионность API
- v2 и v3 работают параллельно
- v3 содержит новые фичи (auth, admin)
- Обратная совместимость для старых клиентов

## 🐛 Отладка и помощь

### Проблемы с подключением к БД
```bash
# Проверьте строку подключения
python test_connection.py

# Проверьте переменные окружения
cat .env

# Проверьте доступность хоста
ping db.example.com
```

### Просмотр логов
```bash
# С выводом debug логов
uvicorn main:app --reload --log-level debug

# Проверка логов планировщика в консоли
```

### Сброс пароля администратора
```python
# Запустите интерпретатор Python
python
>>> from auth_utils import get_password_hash
>>> print(get_password_hash("new_admin_password"))
# Скопируйте хеш и обновите в БД
```

## 📚 Полезные ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy Async](https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html)
- [Pydantic V2](https://docs.pydantic.dev/latest/)
- [PyJWT](https://pyjwt.readthedocs.io/)
- [APScheduler](https://apscheduler.readthedocs.io/)
- [Матрица Эйзенхауэра](https://ru.wikipedia.org/wiki/Матрица_Эйзенхауэра)
- [Supabase](https://supabase.com/docs)

## 📄 Лицензия

Проект создан в образовательных целях для изучения:
- FastAPI и асинхронного программирования
- Проектирования API
- Безопасности веб-приложений
- Системы управления задачами

---

**Версия API**: 3.1.0  
**Версия бэкенда**: 2.1.0  
**Версия Python**: 3.10+  
**Последнее обновление**: 15 декабря 2025 г.
