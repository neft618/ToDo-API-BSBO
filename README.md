# ToDo API с матрицей Эйзенхауэра

REST API для управления задачами с использованием матрицы Эйзенхауэра, разработанное на FastAPI с интеграцией PostgreSQL.

## 📋 Описание

Это полнофункциональное API для управления списком задач (ToDo List) с поддержкой категоризации задач по матрице Эйзенхауэра. Матрица делит задачи на 4 квадранта в зависимости от их важности и срочности:

- **Q1** - Важные и срочные (требуют немедленного внимания)
- **Q2** - Важные, но не срочные (стратегическое планирование)
- **Q3** - Не важные, но срочные (делегирование)
- **Q4** - Не важные и не срочные (можно отложить)

### ✨ Ключевые возможности

- 🗄️ Полная интеграция с PostgreSQL (Supabase)
- 📅 Автоматическое определение срочности на основе дедлайнов
- ⏰ Планировщик задач для автоматического обновления статусов
- 🔄 Полный CRUD для управления задачами
- 📊 Расширенная статистика и аналитика
- 🔍 Поиск и фильтрация задач
- ✅ Валидация данных с помощью Pydantic

## 🚀 Технологии

- **FastAPI** 0.119.0 - современный веб-фреймворк для создания API
- **SQLAlchemy** (async) - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных (Supabase)
- **Pydantic** 2.12.0 - валидация и сериализация данных
- **APScheduler** 3.10.4 - планировщик задач
- **Uvicorn** 0.37.0 - ASGI сервер

## 📦 Установка

### 1. Клонируйте репозиторий
```bash
git clone <url-репозитория>
cd ToDo-API-BSBO
```

### 2. Создайте виртуальное окружение
```bash
python -m venv .venv
source .venv/bin/activate  # для macOS/Linux
# или
.venv\Scripts\activate  # для Windows
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте базу данных

Создайте файл `.env` в корне проекта:
```env
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database
```

Для использования Supabase:
1. Создайте проект на [supabase.com](https://supabase.com)
2. Получите строку подключения в разделе Settings → Database
3. Замените `postgresql://` на `postgresql+asyncpg://` в строке подключения

### 5. Проверьте подключение
```bash
python test_connection.py
```

## 🎯 Запуск

### Режим разработки
```bash
uvicorn main:app --reload
```

### Продакшн режим
```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

API будет доступно по адресу: `http://127.0.0.1:8000`

При запуске автоматически:
- ✅ СоAPI Эндпоинты

### Основные эндпоинты

- `GET /` - Приветственное сообщение и информация об API
- `GET /health` - Проверка здоровья API и подключения к БД

### Управление задачами (`/api/v2/tasks`)

#### Получение задач
- `GET /api/v2/tasks/` - Получить все задачи
- `GET /api/v2/tasks/{task_id}` - Получить задачу по ID
- `GET /api/v2/tasks/quadrant/{quadrant}` - Фильтрация по квадранту (`Q1`, `Q2`, `Q3`, `Q4`)
- `GET /api/v2/tasks/status/{status}` - Фильтрация по статусу (`completed` или `pending`)
- `GET /api/v2/tasks/search?q={keyword}` - Поиск задач (минимум 2 символа)
- `GET /api/v2/tasks/today` - Задачи со сроком на сегодня

#### Создание и изменение
- `POST /api/v2/tasks/` - Создать новую задачу
- `PUT /api/v2/tasks/{task_id}` - Обновить задачу
- `PATCH /api/v2/tasks/{task_id}/complete` - Отметить задачу как выполненную
- `PATCH /api/v2/tasks/{task_id}/uncomplete` - Отметить задачу как невыполненную
- `DELETE /api/v2/tasks/{task_id}` - Удалить задачу

### Статистика (`/api/v2/stats`)

- `GET /api/v2/stats/` - Общая статистика по задачам
- `GET /api/v2/stats/timing` - Статистика по дедлайнам
- `GET /api/v2/stats/compleapi/v2/tasks/
```

### Создать новую задачу
```bash
curl -X POST http://127.0.0.1:8000/api/v2/tasks/ \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Подготовить презентацию",
    "description": "Презентация для защиты проекта",
    "is_important": true,
    "deadline_at": "2025-12-20T15:00:00Z"
  }'
```

### Обновить задачу
```bash
curl -X PUT http://127.0.0.1:8000/api/v2/tasks/1 \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Обновленное название",
    "is_important": false
  }'
```

### Отметить задачу выполненной
```bash
curl -X PATCH http://127.0.0.1:8000/api/v2/tasks/1/complete
```

### Поиск задач
```bash
curl http://127.0.0.1:8000/api/v2/tasks/search?q=презентация
```

### Получить статистику
```bash
curl http://127.0.0.1:8000/api/v2/stats/
```

### Получить задачи на сегодня
```bash
curl http://127.0.0.1:   # Точка входа приложения (lifespan, роуты)
├── database.py          # Настройки подключения к PostgreSQL
├── schemas.py           # Pydantic схемы для валидации
├── models.py            # Файл-импортер моделей
├── models/              # SQLAlchemy модели
│   ├── __init__.py
│   └── task.py         # Модель Task
├── routers/            # API роуты
│   ├── __init__.py
│   ├── tasks.py        # CRUD операции для задач
│   └── stats.py        # Эндпоинты статистики
├── scheduler.py        # Планировщик автообновления срочности
├── utils.py            # Вспомогательные функции
├── test_connection.py  # Скрипт проверки подключения к БД
├── requirements.txt    # Зависимости проекта
├── .env               # Переменные окружения (не в Git!)
├── .gitignore         # Игнорируемые файлы
└── README.md 27.0.0.1:8000/health
```

### Получить статистику
```bash
curl http://127.0.0.1:8000/tasks/stats
```

### Фильтрация по квадранту
```bash
curl http://127.0.0.1:8000/tasks/quadrant/Q1
```

### Фильтрация по статусу
```bash
curl http://127.0.0.1:8000/tasks/status/completed
```

## 📊 Модель данных задачи

### Поля в базе данных

```python
{
  "id": 1,                                    # Уникальный идентификатор
  "title": "Название задачи",                 # Обязательное поле (3-100 символов)
  "description": "Описание задачи",           # Опционально (до 500 символов)
  "is_important": true,                       # Важность задачи
  "is_urgent": true,                          # Срочность (авто-вычисляется)
  "deadline_at": "2025-12-20T15:00:00Z",     # Дедлайн (опционально)
  "quadrant": "Q1",                          # Квадрант матрицы (авто-вычисляется)
  "completed": false,                        # Статус выполнения
  "created_at": "2025-12-14T10:00:00Z",      # Дата создания
  "completed_at": null                       # Дата выполнения
}
```🎨 Особенности реализации

### Асинхронная архитектура
Проект использует асинхронный подход для максимальной производительности:
- Асинхронные запросы к БД через `AsyncSession`
- Асинхронный драйвер `asyncpg` для PostgreSQL
- Неблокирующие операции ввода-вывода

### Планировщик задач
APSc� Отладка

### Проблемы с подключением к БД
```bash
# Проверьте строку подключения
python test_connection.py

# Проверьте переменные окружения
cat .env
```

### Логи планировщика
Проверьте консоль для сообщений о работе планировщика:
```
[2025-12-15 09:00:00] Запуск автоматического обновления срочности задач...
Обновлено задач: 3 из 10
```

## 📚 Полезные ссылки

- [Документация FastAPI](https://fastapi.tiangolo.com/)
- [SQLAlchemy Async](https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html)
- [Pydantic V2](https://docs.pydantic.dev/latest/)
- [APScheduler](https://apscheduler.readthedocs.io/)
- [Матрица Эйзенхауэра](https://ru.wikipedia.org/wiki/Матрица_Эйзенхауэра)

## 👨‍💻 Автор

**Чистяков Сергей**

## 📄 Лицензия

Проект создан в образовательных целях для изучения FastAPI и асинхронного программирования на Python.

---

**Версия API:** 2.1.0  
**Последнее обновление:** 15 декабря 2025 г
- Проверка длины строк
- Валидация типов данных
- Автоматическая генерация документации

## 🔮 Планы развития

- [x] Интеграция с PostgreSQL через SQLAlchemy
- [x] Добавление полного CRUD для задач
- [x] Добавление дедлайнов для задач
- [x] Автоматическое определение срочности
- [x] Расширенная статистика
- [ ] Аутентификация пользователей (JWT)
- [ ] Поддержка тегов и категорий
- [ ] Уведомления о приближающихся дедлайнах
- [ ] Экспорт задач в CSV/JSON
- [ ] Docker контейнеризация
- [ ] CI/CD pipelineрочена ли задача
}
```

### Логика автоматического определения срочности

- **Срочно** (`is_urgent=true`): дедлайн через ≤ 3 дня
- **Не срочно** (`is_urgent=false`): дедлайн через > 3 дня или не указан

Планировщик обновляет срочность автоматически каждые 5 минут и ежедневно в 09:00 UTC.
```json
{
  "id": 1,
  "title": "Название задачи",
  "description": "Описание задачи",
  "is_important": true,
  "is_urgent": true,
  "quadrant": "Q1",
  "completed": false,
  "created_at": "2025-12-14T..."
}
```

## 🔮 Планы развития

- [ ] Интеграция с PostgreSQL через SQLAlchemy
- [ ] Добавление CRUD операций (POST, PUT, DELETE)
- [ ] Аутентификация пользователей
- [ ] Добавление дедлайнов для задач
- [ ] Приоритизация задач внутри квадрантов
- [ ] Экспорт задач в различные форматы

## 👨‍💻 Автор

**Грошев Роман**

## 📄 Лицензия

Проект создан в образовательных целях.