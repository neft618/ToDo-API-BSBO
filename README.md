# ToDo API с матрицей Эйзенхауэра

REST API для управления задачами с использованием матрицы Эйзенхауэра, разработанное на FastAPI.

## 📋 Описание

Это API для управления списком задач (ToDo List) с поддержкой категоризации задач по матрице Эйзенхауэра. Матрица делит задачи на 4 квадранта в зависимости от их важности и срочности:

- **Q1** - Важные и срочные (требуют немедленного внимания)
- **Q2** - Важные, но не срочные (стратегическое планирование)
- **Q3** - Не важные, но срочные (делегирование)
- **Q4** - Не важные и не срочные (можно отложить)

## 🚀 Технологии

- **FastAPI** - современный веб-фреймворк для создания API
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone <url-репозитория>
cd ToDo-API-BSBO
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # для macOS/Linux
# или
venv\Scripts\activate  # для Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

## 🎯 Запуск

Запустите сервер:
```bash
uvicorn main:app --reload
```

API будет доступно по адресу: `http://127.0.0.1:8000`

## 📚 Документация API

После запуска сервера автоматическая документация доступна по адресам:
- Swagger UI: `http://127.0.0.1:8000/docs`
- ReDoc: `http://127.0.0.1:8000/redoc`

## 🔌 Эндпоинты

### Основные

- `GET /` - Приветственное сообщение и информация об API
- `GET /tasks` - Получить все задачи
- `GET /tasks/{task_id}` - Получить задачу по ID

### Статистика и фильтрация

- `GET /tasks/stats` - Получить статистику по задачам (общее количество, по квадрантам, по статусу)
- `GET /tasks/search?q={keyword}` - Поиск задач по ключевому слову (минимум 2 символа)
- `GET /tasks/status/{status}` - Фильтрация по статусу (`completed` или `pending`)
- `GET /tasks/quadrant/{quadrant}` - Фильтрация по квадранту (`Q1`, `Q2`, `Q3`, `Q4`)

## 💡 Примеры использования

### Получить все задачи
```bash
curl http://127.0.0.1:8000/tasks
```

### Поиск задач
```bash
curl http://127.0.0.1:8000/tasks/search?q=FastAPI
```

### Получить статистику
```bash
curl http://127.0.0.1:8000/tasks/stats
```

### Фильтрация по квадранту
```bash
curl http://127.0.0.1:8000/tasks/quadrant/Q1
```

### Фильтрация по статусу
```bash
curl http://127.0.0.1:8000/tasks/status/completed
```

## 📊 Структура проекта

```
ToDo-API-BSBO/
├── main.py           # Основной файл приложения с эндпоинтами
├── models.py         # SQLAlchemy модели (будет добавлено)
├── schemas.py        # Pydantic схемы (будет добавлено)
├── database.py       # Настройки БД (будет добавлено)
├── requirements.txt  # Зависимости проекта
└── README.md         # Документация
```

## 📝 Структура задачи

Каждая задача содержит следующие поля:

```json
{
  "id": 1,
  "title": "Название задачи",
  "description": "Описание задачи",
  "is_important": true,
  "is_urgent": true,
  "quadrant": "Q1",
  "completed": false,
  "created_at": "2025-12-14T..."
}
```

## 🔮 Планы развития

- [ ] Интеграция с PostgreSQL через SQLAlchemy
- [ ] Добавление CRUD операций (POST, PUT, DELETE)
- [ ] Аутентификация пользователей
- [ ] Добавление дедлайнов для задач
- [ ] Приоритизация задач внутри квадрантов
- [ ] Экспорт задач в различные форматы

## 👨‍💻 Автор

**Чистяков Сергей**

## 📄 Лицензия

Проект создан в образовательных целях.